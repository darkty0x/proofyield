providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["bash scripts/railway-build.sh"]

[start]
cmd = "bash scripts/railway-start.sh"
